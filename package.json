{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Personal expense tracker with Telegram bot integration and web dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check database.js && node --check bot.js && node --check routes/api.js && node --check api/index.js && node --check public/app.js && node --check categories.js && node --check test-gemini.js",
    "migrate": "node migrate-sqlite-to-neon.js",
    "test:gemini": "node test-gemini.js"
  },
  "keywords": [
    "expense",
    "tracker",
    "telegram",
    "bot"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "node-telegram-bot-api": "^0.66.0",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
